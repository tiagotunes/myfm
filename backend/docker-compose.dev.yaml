services:
  db:
    container_name: myfm-db
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - '${DB_PORT}:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

  api:
    build: .
    container_name: myfm-api
    command: npm run start:dev
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - '${API_PORT}:3000'
    env_file:
      - .env
    environment:
      NODE_ENV: development
    depends_on:
      - db
    restart: unless-stopped

volumes:
  postgres_data:
